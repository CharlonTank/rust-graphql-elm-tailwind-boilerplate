{
  "name": "rust-graphql-elm-tailwind",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "concurrently \"cd backend && cargo watch -x run\" \"cd frontend && npm start\"",
    "install": "cd frontend && npm install",
    "build": "cd frontend && npm run build && cd ../backend && cargo build --release"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
} 